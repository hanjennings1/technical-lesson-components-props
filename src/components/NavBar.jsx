import React from "react";


/**
 * NavBar renders the site's navigation links.
 * Currently static — does not receive any props.
 */
function NavBar() {
  return (
    <nav>
      <a href="#home">Home</a>
      <a href="#about">About</a>
    </nav>
  );
}
export default NavBar;